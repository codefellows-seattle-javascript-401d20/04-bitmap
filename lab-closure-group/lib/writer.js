'use strict';

const fs = require('fs');

module.exports = (path, object, callback) => {
  // write the object data to the assigned file.

  // Go back to index.js and finish the program.
  callback();
};
